This directory will store the configuration file for your elastic searh server
myconfig.py is automatically created when you install your elasticsearch and kibana server with our scripts (Linux debian or Docker)
Else, you have to fill it manually
